<!doctype html>
<html xmlns="http://www.w3.org/1999/xhtml">
  <head>
    <meta
      name="generator"
      content="HTML Tidy for HTML5 for Apple macOS version 5.8.0"
    />
    <title>Image Converter</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/htmx.org@1.9.10"></script>
  </head>
  <body>
    <div
      class="flex flex-col items-center justify-center min-h-screen p-4 gap-4 text-center"
    >
      <div class="max-w-2xl w-full space-y-4">
        <div class="space-y-2">
          <h1 class="text-3xl font-bold tracking-tighter sm:text-5xl">
            PNG to JPG Converter
          </h1>
          <p
            class="mx-auto max-w-prose text-gray-500 md:text-xl/relaxed text-gray-400"
          >
            Convert your PNG images to high-quality JPG format. Simply select
            the PNG file you want to convert, and we'll take care of the rest.
          </p>
        </div>
        <form
          hx-encoding="multipart/form-data"
          hx-post="/upload"
          hx-target="#result"
        >
          <input accept="image/png" name="file" type="file" />
          <button
            for="file"

          class="border border-dashed border-gray-200 rounded-lg bg-white max-w-sm p-2 my-2 mx-auto w-full border-gray-800 bg-gray-950"
          >
            Convert image
          </button>
        </form>
      </div>
      <div class="p-0">
        <div class="flex items-center justify-center p-6" id="result">
        </div>
      </div>
    </div>
  </body>
</html>
